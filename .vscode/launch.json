{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/reactdoccker/package.json",
                "localRoot": "${workspaceFolder}/reactdoccker"
            }
        }
    ]
}